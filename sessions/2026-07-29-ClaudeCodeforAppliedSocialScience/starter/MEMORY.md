# Memory

<!-- Things learned that stay true across sessions. Data quirks, package
     versions that break things, the reason something is built the way it
     is. Not a diary, and not a summary of what happened. Only what would
     be annoying to work out a second time.

     Keep it under about 100 lines. Prune rather than append forever. -->

## Data

- [e.g. `survey_2015.csv` uses semicolons, every other wave uses commas.]

## Tools and environment

- [e.g. `fixest` 0.12 changed the default `vcov`. Pinned in `docs/environment.txt`.]

## Decisions that are settled

- [e.g. Municipality mergers are handled at 2020 boundaries. Decided 2026-03-14, see session log.]
